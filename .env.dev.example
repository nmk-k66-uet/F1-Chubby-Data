# Local development environment
# Copy to .env: cp .env.dev.example .env

# InfluxDB
INFLUXDB_TOKEN=f1chubby-influx-token
INFLUXDB_PASSWORD=f1chubby2026

# Model Serving — uses local assets/Models/*.pkl via bind mount
# No GCS auth needed for local dev
